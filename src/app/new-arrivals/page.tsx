import { CategoryRedirect } from '@/components/CategoryRedirect';

export default function NewArrivalsPage() {
  return <CategoryRedirect title="NEW ARRIVALS" subtitle="Fresh drops every Friday." badge="new" />;
}
